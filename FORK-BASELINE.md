# Local baseline overlap audit

Read-only inspection at local HEAD `8e2afc09126c0cea4c282725fe68af43adad73d7`, 2026-09-10. Static source evidence, not runtime qualification. All paths relative to repository. These capabilities already exist in this checkout; a fork enhancement should identify the missing behavior, not merely copy a feature name. No source files changed.

| Capability | Status | Evidence and observed limits | Adoption implication |
|---|---|---|---|
| Multiagent history selection | Present | `codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs:286` parses `fork_turns`; lines 298–319 accept none/all/positive integer and reject zero/invalid text. | Better context selection/evaluation could add value; history forking itself is existing. |
| Agent model/effort selection | Present | `core/src/tools/handlers/multi_agents_common.rs:267` (under codex-rs) applies requested or default subagent model and validates available models/effort. `core/src/tools/handlers/multi_agents_v2/spawn.rs:125` applies overrides and role. Runtime instructions in this conversation can impose stricter full-history override rules than the implementation. | Task-aware routing, multi-provider adapters, budgets and quality feedback are distinct improvements. |
| Agent messaging and followups | Present | `codex-rs/core/src/tools/handlers/multi_agents_v2/message_tool.rs:1` documents shared message path; lines 12–23 distinguish QueueOnly from TriggerTurn. `core/src/agent/control/execution.rs:52` enforces execution limit. | Persistent cross-root rooms, dependency DAGs, ownership arbitration require additional design; don't call basic messaging novel. |
| Lifecycle hooks | Present | `codex-rs/hooks/src/schema.rs:102` defines PreToolUse, PermissionRequest, PostToolUse, PreCompact, PostCompact, SessionStart, UserPromptSubmit, SubagentStart, SubagentStop, Stop, Interrupt wire names. `hooks/src/registry.rs:217` onward implements typed dispatch, including session-end at 291. `registry.rs:203` says concurrent permission hooks bounded by maximum configured timeout. | Typed evidence capture, calibration, observability and turnkey integrations should build atop existing hooks. |
| Autonomous goal continuation and budgets | Present | `codex-rs/ext/goal/src/runtime.rs:418` continues idle active goals via persisted thread goal state; exits for deferred goals, unavailable thread manager/live thread, inactive/missing goals. `ext/goal/src/extension.rs:54` has max_goal_token_budget; line 510 handles budget-limit reporting. | Better stop criteria, verified completion, per-stage budgets and durable recovery are extensions. This is not evidence of a daemon that restores all work after process death. |
| Queue messages from CLI | Present | `codex-rs/cli/src/queue_cmd.rs:12` targets session UUID or exact name, accepts text; line 45 explicitly rejects images; line 48 resolves optional remote endpoint. `cli/src/main.rs:207` registers queue command. | Rich attachments, scheduling and dependency-aware inboxes remain plausible extensions. |
| Retry/reconnect | Present, bounded | `codex-rs/codex-client/src/retry.rs:22` handles configured 429/5xx/transport retries with max attempts; line 80 runs retry/backoff. `core/src/client.rs:2016` describes fallback after exhausting provider retry budget. | Avoid presenting plain retry as new. Durable tool-effect reconciliation or configurable long outage recovery needs deeper inspection. |
| Themes | Present | `codex-rs/tui/src/theme_picker.rs:3` and `:304` support bundled and custom .tmTheme files with live preview. | Semantic workflow views matter more than another color picker. |
| Usage/statusline | Present, cost scope limited | `codex-rs/tui/src/bottom_pane/status_line_setup.rs:82` git branch, `:123` used tokens, `:126` input/output, `:134` estimated cost. Lines 199–203 state credits/cost enterprise only and omitted when unavailable. | Provider-independent cost, agent-tree attribution and budget projections would extend actual limits. |
| Compaction | Present | `codex-rs/core/src/compact.rs:114` inline auto compaction; `:146` manual task; `:185` and `:212` pre/post hooks; `:80` persisted checkpoint metadata. | Context provenance, selective retrieval, regression tests for retained decisions remain more specific ideas. |
| Code-mode tool orchestration | Present | `codex-rs/code-mode-runtime/src/runtime/globals.rs:15` V8 globals; tools and ALL_TOOLS, store/load, yield_control, timers, rich outputs. Explicitly removes console, Atomics, SharedArrayBuffer, WebAssembly. | RLM/document substrate needs more than JS tool batching; reusable retrieval stores and verified recursive partitioning are distinct. |
| Plugins + marketplaces + MCP | Present | `codex-rs/core-plugins/src/lib.rs:1` has loader, marketplaces add/remove/upgrade, remote bundles, recommendation and startup synchronization. `codex-rs/codex-mcp/src/connection_manager/tool_catalog.rs:133` lists tools, `:446` refreshes app discovery. | Compatibility, trust provenance, rollback and extension author tooling are extensions; plugin support is not novel. |
| Managed session worktrees | Present | `codex-rs/tui/src/worktree_startup.rs:1` manages allocation, trust, binding and failed-start recovery. `cli/src/main.rs:2486` validation: new interactive/exec sessions and explicit forks supported; existing-session resume with --worktree rejected; code review unsupported. `git-utils/src/worktree.rs:93` enumerates linked worktrees. | Automatic worktree-per-child plus merge/conflict arbitration is a stronger proposal than merely adding worktrees. No worktree references found in multiagent handler/agent paths searched; this is a scoped search result, not proof none exists elsewhere. |
| Memory foundations | Present | `codex-rs/memories/read/src/lib.rs:1` owns memory injection, citations and read telemetry; separate write crate exists. | Avoid claiming memory itself is absent; project-scoped temporal evidence, retrieval quality and user controls require direct comparison. |

Best general framing: retain the current core primitives and adopt higher-level behavior: measurable completion gates, durable recovery, task-aware routing, agent ownership/merge coordination, provenance-aware context, calibrated memory, and fork upkeep automation. None of the above baseline findings implies a proposed higher-level feature is absent without a targeted follow-up audit.
